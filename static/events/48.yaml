# Random event - Grade released on home assignment.
event:
  id: 48
  descriptions:
    - "You got the email notification about the grade released on your last homework assigment.<br>
      {{ IF goodGrade }}Your last submission was perfect and got a good grade.
      {{ ELSE }}
        {{ IF badGrade }}Your last submission is chaos and got a bad grade.
        {{ ELSE }}Your last submission was good, and got a average grade.
        {{ FI }}
      {{ FI }}"
  options:
    307: "{{ IF goodGrade }}Nicely done.{{ ELSE }}{{ IF badGrade }}Should do it better next time..{{ ELSE }}Cool.{{ FI }}{{ FI }}"